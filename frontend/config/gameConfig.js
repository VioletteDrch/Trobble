export const sizes = {
  width: 320,
  height: 500,
};

export const gameState = {
  points: 0,
  pileSize: 1,
  playerName: "andre",
  activeAnimations: [],
};

export const gameRules = {
  totalAmountOfCards: 10,
  pilePosition: { x: 150, y: 130 },
};
