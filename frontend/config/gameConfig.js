export const sizes = {
  width: 300,
  height: 500,
};

export const gameState = {
  points: 0,
  pileSize: 1,
  playerName: "andre",
  activeAnimations: [],
};

export const gameRules = {
  totalAmountOfCards: 10,
  pilePosition: { x: 400, y: 100 },
};
